from setuptools import setup, find_packages

setup(
    name="testpkg",
    version="0.1.0",
    packages=find_packages(),
    description="A minimal package for composite action testing.",
    author="Cline",
    install_requires=[],
)
